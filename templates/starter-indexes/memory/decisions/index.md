# Decisions Index

Record of meaningful decisions made across all projects. Architecture choices, tool selections, approach decisions, and strategic directions.

## Recent Decisions

<!-- AUTO-UPDATED: one line per decision (date, bold title, project+type, doc link). Full detail lives in the dated file, never inline here. Keep this lean (context-budget discipline). -->

## How This Works

- Decisions are auto-logged when you weigh options and make a choice (see workflows.md).
- Each decision gets its own file: `YYYY-MM-DD-short-topic.md` holding the full Context / Options / Decision / Consequences. **This index carries only a one-line pointer per decision.**
- Keep this to ~50 recent one-liners; `/prune` archives older ones to `_archive-index.md` (the dated files stay in place).
- Say "log this decision" to manually capture one.
